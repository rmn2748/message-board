extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") write your own message

  div.message
    each message in messages
      h2=  message.user
      p= message.text
      p= message.added